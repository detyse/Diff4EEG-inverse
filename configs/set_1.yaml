model:
    # 可将scheduler和sampler放入config
    # type:

net:
    in_chn: 3
    num_tokens: 32
    depth: 24
    # default parameters
    dropout: 0
    embedding_dim: 128
    transpose: True

s4:
    mode: 'nplr'
    measure: 'legs'
    bidirectional: True
    # d_state: 64
    transpose: True


train:
    lr: 0.00001
    batch_size: 50
    epoch: 100
    save_path: 'projects/_BSS_hijack/result'


sampling:
    batch_size:
    